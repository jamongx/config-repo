spring:
  datasource:
    url: jdbc:postgresql://localhost:6432/erp_db
    username: admin_erp
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: true
  cache:
    type: caffeine
